- JOB: "intercept -g $DEVNODE \
        | dual-function-keys -c /etc/interception/dual-function-keys/trackpoint.yaml \
        | uinput -d $DEVNODE"
  DEVICE:
    NAME: "Elan TrackPoint"     # match your device; adjust if the name differs
    EVENTS:
      EV_KEY: [ BTN_MIDDLE ]

